services:
  catstats:
    build: .
    ports:
      - "9090:9090"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_URL=file:/data/catstats.db
      - TZ=America/Chicago
    restart: unless-stopped
